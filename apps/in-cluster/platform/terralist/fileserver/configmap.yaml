apiVersion: v1
kind: ConfigMap
metadata:
  name: terralist-fileserver-nginx
data:
  default.conf: |
    server {
      listen 8080;
      server_name _;
      root /srv;
      autoindex on;
      location / {
        try_files $uri $uri/ =404;
      }
    }
