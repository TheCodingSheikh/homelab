apiVersion: vault.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: vault-provider-config
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  address: http://vault.vault.svc:8200
  credentials:
    secretRef:
      key: credentials
      name: vault-creds
      namespace: crossplane
    source: Secret
---
apiVersion: v1
kind: Secret
metadata:
  name: vault-token
  namespace: crossplane
stringData:
  credentials: |
    {
      "token": "hvs.GalR6fWZmFMCVoLuU3b8JPcK"
    }
