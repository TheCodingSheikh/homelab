apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - job.yaml

# Hash-suffixed so editing the plan produces a new ConfigMap name, which
# kustomize rewrites into the Job's volume reference.
configMapGenerator:
  - name: stalwart-plan
    files:
      - plan.yaml
