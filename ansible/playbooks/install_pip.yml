- hosts: lab
  become: true

  tasks:
    - name: Ensure pip is installed
      apt:
        name: python3-pip
        state: present

    - name: Install kubernetes Python library
      pip:
        name: kubernetes
        state: present
        executable: pip3